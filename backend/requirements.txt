# CP6.6：OpenAPI schema 工具链依赖（dev-tools，不是 4 个服务的运行时依赖）。
#
# 4 个服务各自的运行时依赖仍在 backend/<service>/requirements.txt；
# 这里只放「生成 / 校验 OpenAPI schema」相关的工具依赖。
#
# 安装：backend/.venv/bin/pip install -r backend/requirements.txt

# OpenAPI 3.1 schema 校验（backend/tests/test_openapi_export.py 用）
openapi-spec-validator>=0.7.0
